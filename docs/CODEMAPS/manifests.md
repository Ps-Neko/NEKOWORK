# CODEMAP — manifests

> 자동 생성. `scripts/build-codemaps.js` 가 `manifests/` 를 스캔. 직접 편집 금지.
> 코드 본문은 포함 안 함. 네비게이션 보조용.

## 디렉터리 트리

```
manifests/
├── install-components.json
├── install-modules.json
└── install-profiles.json
```

