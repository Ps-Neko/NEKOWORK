diff --git a/.nekowork-capture/session.json b/.nekowork-capture/session.json
new file mode 100644
index 0000000..4cc1df1
--- /dev/null
+++ b/.nekowork-capture/session.json
@@ -0,0 +1,9 @@
+{
+  "started_at": "2026-05-27T06:05:31.851Z",
+  "workspace": "C:/Users/ILJIN/AppData/Local/Temp/nekowork-live-ai-session-001",
+  "tool": "claude-code",
+  "model": "opus-4.7",
+  "task_id": "tier1-jwt-auth-001",
+  "prompt": "Add JWT-based auth middleware to this Express app.",
+  "starting_sha": "44e1c08d64d7a417997034a975ba9e855b627751"
+}
\ No newline at end of file
diff --git a/index.js b/index.js
index 4c98daa..a57772f 100644
--- a/index.js
+++ b/index.js
@@ -1,4 +1,5 @@
 import express from 'express';
+import { authMiddleware, signToken } from './middleware/auth.js';
 
 const app = express();
 app.use(express.json());
@@ -7,5 +8,19 @@ app.get('/', (req, res) => {
   res.json({ ok: true });
 });
 
+// Demo login route — in production, replace with real credential verification
+app.post('/login', (req, res) => {
+  const { username } = req.body || {};
+  if (!username) {
+    return res.status(400).json({ error: 'username required' });
+  }
+  const token = signToken({ sub: username });
+  res.json({ token });
+});
+
+app.get('/protected', authMiddleware, (req, res) => {
+  res.json({ ok: true, user: req.user });
+});
+
 const PORT = process.env.PORT || 3000;
 app.listen(PORT, () => console.log(`listening on :${PORT}`));
diff --git a/middleware/auth.js b/middleware/auth.js
new file mode 100644
index 0000000..53a4d82
--- /dev/null
+++ b/middleware/auth.js
@@ -0,0 +1,23 @@
+import jwt from 'jsonwebtoken';
+
+const JWT_SECRET = process.env.JWT_SECRET || 'dev-secret-change-in-production';
+const JWT_EXPIRES_IN = process.env.JWT_EXPIRES_IN || '24h';
+
+export function signToken(payload) {
+  return jwt.sign(payload, JWT_SECRET, { expiresIn: JWT_EXPIRES_IN });
+}
+
+export function authMiddleware(req, res, next) {
+  const auth = req.headers.authorization;
+  if (!auth || !auth.startsWith('Bearer ')) {
+    return res.status(401).json({ error: 'missing or malformed Authorization header' });
+  }
+  const token = auth.slice('Bearer '.length).trim();
+  try {
+    const decoded = jwt.verify(token, JWT_SECRET);
+    req.user = decoded;
+    next();
+  } catch (err) {
+    return res.status(401).json({ error: 'invalid or expired token' });
+  }
+}
diff --git a/package.json b/package.json
index 031e6b3..df6f1b7 100644
--- a/package.json
+++ b/package.json
@@ -3,6 +3,7 @@
   "version": "0.0.1",
   "type": "module",
   "dependencies": {
-    "express": "^4.21.0"
+    "express": "^4.21.0",
+    "jsonwebtoken": "^9.0.2"
   }
 }
