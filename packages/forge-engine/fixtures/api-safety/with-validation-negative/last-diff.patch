diff --git a/src/api/update-profile.ts b/src/api/update-profile.ts
@@ -1,4 +1,8 @@
 import { z } from "zod";
+const schema = z.object({ displayName: z.string().min(1), bio: z.string().max(500) });
 export async function updateProfile(req: Request) {
+  const input = schema.parse(req.body);
+  return create(input);
   return next(req);
 }
diff --git a/tests/api/update-profile.test.ts b/tests/api/update-profile.test.ts
@@ -1,3 +1,4 @@
 import { test } from "node:test";
 test("update: pass", () => { /* stub */ });
+test("update: validation rejects empty name", () => { /* stub */ });
