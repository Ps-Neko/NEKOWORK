diff --git a/src/util/parse.ts b/src/util/parse.ts
@@ -1,3 +1,7 @@
 export function parse(input: string): { ok: boolean; value: string } {
   return { ok: true, value: input.trim() };
 }
+
+export function parseStrict(input: string): { ok: boolean; value: string } {
+  return parse(input.toLowerCase());
+}
diff --git a/tests/util/parse.test.ts b/tests/util/parse.test.ts
@@ -1,4 +1,7 @@
 import { test } from "node:test";
 import { parse } from "../../src/util/parse.js";
 test("parse: basic", () => { parse(""); });
+test("parseStrict: basic", () => {
+  /* stub */
+});
